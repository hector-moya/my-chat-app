{
  "name": "my-chat-app",
  "version": "1.0.0",
  "description": "Assignment for Software Frameworks",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd server && node server.js\" \"cd client && npm run start\"",
    "install": "cd ./server && npm install && cd ../client && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
